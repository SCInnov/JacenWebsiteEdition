# ⚠️ IMPORTANT: All Work is on index.html

## Main File
**`index.html`** ← This is the MAIN file we're working on
- Uses external CSS files (`css/product.css`, etc.)
- Uses external JS files (`js/product.js`, etc.)
- This is the file being served by the server

## Backup Files
**`BACKUP - Building a Website Step by Step.html`** ← This is a BACKUP only
- Has embedded CSS (old version)
- Do NOT edit this file
- This is just for reference/backup

## All Changes Should Be Made To:
- ✅ `index.html` - Main HTML file
- ✅ `css/product.css` - Product section styles
- ✅ `css/responsive.css` - Responsive styles
- ✅ `js/product.js` - Product section JavaScript
- ✅ Other files in `css/` and `js/` folders

## Do NOT Edit:
- ❌ `BACKUP - Building a Website Step by Step.html`
- ❌ Any other `.html` files (they're backups/old versions)

## Server Configuration
The server (`server.py`) is configured to serve `index.html` by default when you visit `http://localhost:8080`

## Quick Reference
- **Main file**: `index.html`
- **CSS**: `css/product.css`
- **JavaScript**: `js/product.js`
- **Server**: `python3 server.py` (serves index.html)
